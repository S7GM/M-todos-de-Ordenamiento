# Tema: Iserción_InsertionSort
# Nombre: Sofia Galilea Morales Bejarano

# Objetivo de este código:
# En el presente código se mostrará el método de INSERCIÓN, el cual sirve para que los elementos que van a ir ordenados 
# son considerados uno a la vez.

for i in range (len(nums)):
  min_idx=i
  for j in range  (i+1,len (nums)):
  if nums [j] < nums[min_idx], nums[i]
